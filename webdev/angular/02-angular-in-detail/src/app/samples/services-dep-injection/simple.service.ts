import { Injectable } from "@angular/core";

/**
 * A simple service used to illustrate that services can also define dependencies
 * to other services. See discount.service.ts for example of this.
 */
@Injectable()
export class SimpleService {
}
