extends layout
append content
  if paragraphs && paragraphs.length > 0
    .row.banner
      .col-12
        span.page-heading #{pageHeader.title}
    .row
      .col-12.col-lg-8
        each paragraph in paragraphs
          p= paragraph
